# 개선 조치 항목

- 작성일: 2026-09-19
- 원칙: **문제가 있더라도 논문 방식으로 먼저 구현한다.** 논문과 다르게 고치면 나아질 것으로 보이는 항목은 여기에 모아 두고, 구현 후 측정 결과를 보고 착수 여부를 정한다.
- 관련: [01_problem_definition.md](01_problem_definition.md), [kb_build_proposal.md](kb_build_proposal.md), [inferred_prompts.md](inferred_prompts.md)

| ID | 영역 | 현재 (논문대로 진행) | 개선 조치 | 착수 판단 근거 |
|---|---|---|---|---|
| IMP-01 | 그래프 구축 | G1: LLM이 청크마다 개념·관계를 추출 | G2(구조 기반) 또는 G3(G2 + LLM 추출 보강)로 전환 | 그래프 경로가 RRF 최종 결과에 기여한 비율, 같은 입력으로 다시 구축했을 때 그래프가 달라지는 정도 |
| IMP-02 | Critic | 세 기준 중 하나라도 미달이면 재시도, 3회를 넘기면 Native 폴백 | 질의 중요도에 따라 검열 강도를 조절하는 동적 임계치 (논문 9.4절 향후 과제) | 폴백률, Critic 1회 통과율 |
| IMP-03 | 지식베이스 범위 | 전자상거래법 + 분쟁해결기준만. 범위 밖 질문도 특별 처리 없음 | 인용 빈도가 높은 법령(민법·방문판매법·약관규제법 등)을 추가하거나 범위 밖 안내를 추가 | CSV 답변의 27.6%가 범위 밖. 범위 밖 질문의 L2 점수 |
| IMP-04 | 답변 구조 | 논문 프롬프트: 문서 밖 추가 안내 금지. 결론 형식 지시 없음 | 소비자가 원하는 순서(가능/불가 → 대안 → 행동 → 근거)를 지시하는 프롬프트 세트로 교체 (DC-2) | '추가 안내 금지' 때문에 대안이 빠지는 사례 수 |
| IMP-05 | 근거 인용 | 조항 단위 인용 지시 없음 (Agentic은 조항 번호를 드물게만 인용) | 법령명·조항 번호·별표 행을 명시하게 지시하고, 인용한 조항이 실제로 있는지 검증 | L2 '법적 근거' 점수 |
| IMP-06 | Native RAG | "정보가 없으면 없다고 답하라" 지시로 거절이 과다함 | (비교군이므로 원칙적으로 유지) 필요하면 거절 조건을 완화한 변형을 별도 비교군으로 추가 | Native '정보 없음' 응답률 |
| IMP-07 | 모델 | 모든 노드에 gpt-4o-mini | 생성 노드만 상위 모델로 바꾼 비교 실험 (논문 Table 1의 모델 축 재현) | 4o-mini에서 Agentic ≤ Vanilla(인지 부하 역설)로 나오는지 |
| IMP-08 | 평가 | 자동 평가 없음. 샘플 5건 육안 비교 | 저장된 로그(질문·답변·검색 문맥, DC-10)로 L1~L3를 **운영자 키로 오프라인 배치 채점**. 서비스 로직은 바꾸지 않음. L2 심사 모델은 생성 모델보다 상위로 둠 (논문 5.2.3절: 4o-mini 심사관의 긍정 편향) | 논문 수치와 정량 비교가 필요할 때 |
| IMP-09 | 테스트 데이터 | CSV는 제품 밖 수동 비교에만 사용 | 1,332건 일괄 실행 후 표준답변과 비교. 2026.7 법률 개정으로 달라진 문항은 표시하거나 제외 | IMP-08을 착수할 때 |
| IMP-10 | 관리자 로그인 | `admin` / `admin` 하드코딩 | 계정 정보를 환경변수로 옮기고 비밀번호를 해시로 저장 (6차 MVP 단계의 환경변수 점검과 함께) | 공개 URL로 배포하기 전 |
| ~~IMP-11~~ | 대화 | ~~싱글턴~~ | **완료 (2026-09-21).** 논문 3.4절대로 `MemorySaver` 세션 메모리를 붙여 같은 대화에서 분쟁 대상을 이어받는다. 장기 기억은 논문 9.3절의 한계를 따라 두지 않는다 | — |
| IMP-13 | 도구 호출 | 도구 없음 (논문 4.1절이 최종 평가에서 제외한 구성과 같음). 라우터는 2분기 | 논문 부록의 3분기 라우터(`system_action`)와 Lightweight ReAct + Mock 주문 DB를 기능 플래그 뒤에 구현해, 켰을 때 논문 9.2절의 '도구 편향'이 재현되는지 확인 | 논문 9.2절 재현 실험을 할 때. 공개 제품에서는 주문 DB가 없으므로 기본은 꺼 둔다 |
| IMP-17 | DB 스키마 | 기동 시 `create_all` + 뒤늦게 추가된 열만 `ALTER TABLE`로 보정 (`app/db/session.py` LATE_COLUMNS) | Alembic 마이그레이션으로 옮긴다 | 공개 URL로 배포하기 전 |
| IMP-16 | 관측 | 자체 계측만 사용. LangSmith·Langfuse 미사용 | 프로덕션에 Langfuse(자체 호스팅 또는 클라우드)를 붙여 트레이스 디버깅. 질문 원문 전송 범위와 보관 기간을 먼저 정해야 함 | 원인을 찾기 어려운 장애가 반복될 때 |
| IMP-14 | 무료 한도 | Neon 무료 저장 0.5GB(요청 약 1만 건). 80% 도달 시 경고만 | 오래된 검색 스냅샷부터 정리하거나 유료 플랜으로 전환 | 저장 용량 80% 도달 |
| IMP-15 | 콜드 스타트 | Cloud Run 최소 인스턴스 0. 쉬면 첫 응답이 느림 | 최소 인스턴스 1로 상시 가동(유료) 또는 주기적 ping | 시연·발표 일정이 잡혔을 때 |
| IMP-12 | 표 파싱 실패 표 | 검증에 실패한 표는 표 전체를 청크 1개로 넣음 | 실패 목록의 표만 규칙을 보강하거나 사람이 검수 | 실패 목록의 표 수 |

## 항목 추가 규칙

- 구현 중 논문 방식의 문제를 발견하면 **바로 고치지 않고** 여기에 행을 추가한다.
- 착수 판단 근거에는 대시보드나 평가 결과로 확인할 수 있는 지표를 적는다.
